# ================================
# RAILWAY ENVIRONMENT VARIABLES
# ================================
# Copy these to your Railway project Variables section

# Application Configuration
APP_NAME=Elandra E-commerce
APP_ENV=production
APP_KEY=base64:IPo9E5RoKbuRKfmRGJSF2Ur7XxOIcruY0p/aSVCdlrY=
APP_DEBUG=false
APP_URL=https://elandra-production.up.railway.app
APP_TIMEZONE=UTC

# Locale
APP_LOCALE=en
APP_FALLBACK_LOCALE=en

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=info

# Database Configuration
# NOTE: Add MySQL service in Railway - these will be auto-provided:
# MYSQLHOST, MYSQLPORT, MYSQLDATABASE, MYSQLUSER, MYSQLPASSWORD
DB_CONNECTION=mysql

# MongoDB Configuration  
# NOTE: Add MongoDB service in Railway - these will be auto-provided:
# MONGOHOST, MONGOPORT, MONGOUSER, MONGOPASSWORD
MONGO_DB_DATABASE=elandra_products

# Session & Cache (using files for simplicity)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# Cache & Queue (simplified for Railway)
CACHE_STORE=file
QUEUE_CONNECTION=sync
FILESYSTEM_DISK=public

# Mail Configuration (using your Gmail SMTP)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=elandra.live@gmail.com
MAIL_PASSWORD=qawvpmdnlhfotwst
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=elandra.live@gmail.com
MAIL_FROM_NAME=Elandra

# Broadcasting
BROADCAST_CONNECTION=log