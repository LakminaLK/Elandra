# 🚂 Railway Environment Variables Configuration
# Copy these variables to your Railway project dashboard

# Application Configuration
APP_NAME=Elandra
APP_ENV=production
APP_KEY=base64:IPo9E5RoKbuRKfmRGJSF2Ur7XxOIcruY0p/aSVCdlrY=
APP_DEBUG=false
APP_URL=https://elandra-production.up.railway.app
APP_TIMEZONE=Asia/Colombo

# Locale Settings
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Application Settings
APP_MAINTENANCE_DRIVER=file
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Logging Configuration
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database Configuration - MySQL (Railway will provide these automatically)
DB_CONNECTION=mysql
# Railway will automatically provide:
# DB_HOST=${MYSQLHOST}
# DB_PORT=${MYSQLPORT}
# DB_DATABASE=${MYSQLDATABASE}
# DB_USERNAME=${MYSQLUSER}
# DB_PASSWORD=${MYSQLPASSWORD}

# MongoDB Configuration - Products (Railway will provide these automatically)
MONGO_DB_DATABASE=elandra_products
# Railway will automatically provide:
# MONGO_DB_HOST=${MONGOHOST}
# MONGO_DB_PORT=${MONGOPORT}
# MONGO_DB_USERNAME=${MONGOUSER}
# MONGO_DB_PASSWORD=${MONGOPASSWORD}

# Session Configuration (file-based for simplicity)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# Cache and Queue (file-based for simplicity)
CACHE_STORE=file
QUEUE_CONNECTION=sync
BROADCAST_CONNECTION=log

# File Storage
FILESYSTEM_DISK=public

# Mail Configuration (Gmail SMTP)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=elandra.live@gmail.com
MAIL_PASSWORD=qawvpmdnlhfotwst
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=elandra.live@gmail.com
MAIL_FROM_NAME=Elandra

# Vite Configuration
VITE_APP_NAME=Elandra